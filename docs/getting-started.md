# Getting started with ethereum-wallet-toolkit

A collection of Ethereum wallet tools: Python CLI utilities (`eth_toolkit.py`, `wallet.py`, `keystore.py`, `sign.py`, `transaction.py`, `typed_data.py`, `validate.py`, `vanity.py`), five Model Context Protocol (MCP) servers exposing wallet functionality to AI assistants, a fully offline single-file HTML wallet (`offlin

## Install

```bash
npm install
```

## Verify the install

Clone the repository and run its checks to confirm everything works on your machine:

```bash
git clone https://github.com/nirholas/ethereum-wallet-toolkit.git
cd ethereum-wallet-toolkit
```



## Next steps

- [Examples](./examples.md) shows runnable snippets.
- The [README](https://github.com/nirholas/ethereum-wallet-toolkit#readme) is the complete reference.
- Found a problem? [Open an issue](https://github.com/nirholas/ethereum-wallet-toolkit/issues).
